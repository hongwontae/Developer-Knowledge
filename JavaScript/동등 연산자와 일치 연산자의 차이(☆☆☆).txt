1. 동등 연산자 (==)
=> 두 값을 비교할 떄 타입 변환을 자동으로 수행한 뒤 비교합니다.


2. 일치 연산자 (===)
=> 타입 변환 없이 두 값을 직접 변환합니다.


면접 질문
동등 연산자와 일치 연산자의 차이점
=> 두 값을 비교할 떄 동등 연산자는 타입 변환을 수행한 뒤 비교하고 일치 연산자는 타입 변환 없이 비교합니다.
=> 일치 연산자를 사용하는 것이 JS 코드 예측 가능성과 안정성을 높이기 떄문에 일치 연산자를 주로 사용했던 것 같습니다.