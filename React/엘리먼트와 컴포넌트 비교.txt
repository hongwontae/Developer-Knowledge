1. 엘리먼트는 UI 구성 요소를 정의하며, 단순히 HTML 요소를 표현하는 불변하는 JS 객체입니다. 

2. 컴포넌트는 UI의 재사용 가능한 조각을 정의하는 함수 또는 클래스입니다. 컴포넌트가 엘리먼트를 사용하여 UI를
구축합니다.


면접 질문
엘리먼트와 컴포넌트의 차이
=> 엘리먼트는 UI의 기본 구성 단위로 단순히 HTML 요소를 표현하는 불변하는 JS 객체입니다.
=> 컴포넌트는 엘리먼트들을 반환하여 자신의 UI를 구축합니다.