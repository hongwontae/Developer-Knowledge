1. 문자열과 배열을 받아서 배열 요소를 인덱스로 하여 문자열과 일치하는 부분을 삭제한다.


2. join('')
=> ''은 그냥 붙여버리는 것입니다.
=> join()은 ,로 붙어버립니다.


3. replaceAll(1,2)
=> 1에 해당하는 모든 문자열을 2로 바꾸어 버립니다.
=> 원본 문자열을 바꾸지 않고 새로운 문자열을 반환합니다.


4. 나의 풀이
=> map으로 문자열 - index 처리하여 해당되는 부분을 0으로 바꾼다.
=> filter로 0인것을 빼버린다.
=> 그리고 join으로 합친다.


5. 다른 사람 풀이
=> string 길이만큼 for문을 작성한다.
=> if(!a.include(i)){ answer += m[i]} 처리
=> 일치하면 추가할 필요가 없으니 더 하지 않고 일치하지 않으면 더하는 방법으로
=> 세련된 방법이다.